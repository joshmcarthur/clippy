en:
  activerecord:
    errors:
      models:
        upload:
          attributes:
            file:
              blank: "must be provided"
              invalid_content_type: "must be a valid video or audio file"
  uploads:
    create:
      success: "Upload successful"
    update:
      success: "Upload updated"
    destroy:
      success: "Upload deleted"
