<% @upload.summary&.entities.each do |entity| %>
  <%= link_to "#{entity["name"] || entity["entity"]} (#{entity["type"]})", search_uploads_path(q: entity["name"] || entity["entity"]), class: "badge fs-6 my-2 bg-transparent text-decoration-none border text-wrap" %>
<% end %>